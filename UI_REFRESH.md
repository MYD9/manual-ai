# 工作空间 UI 重构

本轮使用“更好的 UI / tactile-ui”v2 规则，保留暖白与橄榄绿品牌，重构资料库、导航、卡片与说明书工具区。复用 React、Base UI、Motion 和 dnd-kit，无新增依赖。

## 文件与界面

- `components/manual-app.tsx`：语义化概览、最近更新入口、明确搜索按钮、当前导航标记、标准按钮形式的卡片打开入口。收藏按钮保持独立；默认排序文案改为实际行为“收藏优先”。
- `app/workspace-refresh.css`：侧栏分区、标题和操作层级、卡片/列表双布局、资料颜色标识、窄屏与触摸目标、高对比支持。
- `app/globals.css`：引入工作空间样式；保留阅读器和章节整理样式。
- `lib/motion-tokens.json` / `app/motion-tokens.css`：统一参数与生成产物。
- `components/chapter-list.tsx`：精确排序直接落到有效位置，移除释放惯性尾段和不再需要的逐帧速度采样。

## Motion 与状态矩阵

| 对象 | 状态与反馈 |
| --- | --- |
| 按钮 | hover 只在精确悬停设备改变背景/层级；press 使用共享 0.97 比例；disabled 失去阴影与按压；键盘焦点清晰 |
| 资料卡片 | 默认暖白表面与分类色书脊；hover 描边/阴影；按压使用既有 cardPress 0.996；标题是真实按钮，整卡可点；收藏单独操作 |
| 搜索 | 输入即时更新；独立提交按钮；保留 Enter、取消旧请求、清空、加载/空结果/错误与重试 |
| 导航与视图 | 当前导航 aria-current；卡片/列表选择 aria-pressed；同一条目保持稳定 ID 与位置布局过渡 |
| 章节排序 | 拖起、阻力、吸附、取消、保存/失败恢复保持；放手后直接收敛目标，不再额外滑过目标 |
| 浮层 | 继续使用 Base UI 的焦点、遮罩与关闭流程；进出场共用强 ease-out |

共享时间保持 fast 100ms / normal 180ms / slow 280ms。enter、exit 曲线更新为 cubic-bezier(0.23, 1, 0.32, 1)，hover scale 从 1.002 改为 1，press 从 0.98 改为 0.97。保留 standard 曲线以及 snappy / soft / heavy / drag 的既有物理参数，不增加随机 spring。

列表位置连续性继续使用 soft spring；其他控件的已有重量分工保持。snap / resistance 仅在已有章节拖动中使用，精确排序不使用惯性。保留用户可关闭、默认关闭的语义 haptic，本轮没有增加声音或新的震动触发点。

系统 reduced motion 继续取消非必要位移/按压/动画，Motion 布局动画关闭；保留拖动位置、选中状态、边框和焦点。新表面使用不透明背景，不依赖 blur 或透明效果。触摸控件至少 44px，hover 效果限定支持 hover 的设备。

## 验证与交接

本地版类型检查、11 项现有前端测试及生产构建通过。浏览器版类型检查、17 项现有前端测试及 Pages 构建通过。统一 tokens 生成一致性检查通过。没有修改后端接口、数据库、附件或 AI 配置，没有用正式资料生成测试数据。公开仓库仅包含通用代码与本说明。

未执行真实浏览器视觉验收、触屏/读屏和低性能设备性能测量，不以构建或静态检查代替这些验证。现有生产包仍有大分块提示。

## 后续优先级

1. 真机验证手机侧栏、触摸排序与键盘焦点。
2. 按需加载 PDF 阅读器和富文本编辑器，减少首屏包体积。
3. 大量说明书/章节下的可见区域渲染与列表性能。
4. 导入队列在慢网络、取消和恢复过程中的细节验收。
5. PDF 长文阅读与 AI 引用之间的定位和返回体验。
